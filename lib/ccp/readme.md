# CCP Protocol - all platforms

This "Chip-toChip Protocol" defines the messages "on the wire" between the click module and
the host microcontroller.

It currently only supports the "FT click" module
